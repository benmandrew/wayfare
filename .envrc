use flake

# The same .env Compose reads, so a local `wayfare` run and a containerised one
# see the same WAYFARE_REGION and WAYFARE_OSM_URL. Without this the two disagree
# silently, which is the one mismatch nothing in the pipeline detects.
dotenv_if_exists .env
